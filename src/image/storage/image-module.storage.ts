export const IMAGE_MODULE_STORAGE: Map<{ originalField: string; entityName: string }, { isArray: boolean; path: string }> = new Map<
  { originalField: string; entityName: string },
  { isArray: boolean; path: string }
>();
